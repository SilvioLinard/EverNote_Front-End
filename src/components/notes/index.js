import React, { Fragment } from "react";
import { Columns, Button, Box } from "react-bulma-components";
import "../../styles/notes.scss";

function Notes() {
  return (
    <Fragment>
      <Columns className="notes" gapless>

  
      </Columns>
    </Fragment>
  );
}

export default Notes;
